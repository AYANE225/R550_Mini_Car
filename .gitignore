__pycache__/
*.pyc
# 可复现的中间数据/缓存（脚本可重生成）——不入库
reports/*.npz
reports/*.npy
reports/*.pkl
reports/*.log
reports/*.pth
reports/*.onnx
reports/*.engine
reports/*.trt
reports/*.plan
# 编译产物 / 大体积第三方(header-only,用 native/fetch_deps.sh 或 apt 获取)
*.so
third_party/Eigen/
# 集成的主流开源模型代码 + 预训练权重（外部获取，不入库；见 semseg/opensource.py 用法）
third_party/WaffleIron/
third_party/*.tar.gz
third_party/weights/
# ROS2 colcon 构建产物 + 录制的 bag（可重建/重录）
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/
reports/ros2_demo_bag/
